자바스크립트(JavaScript)

1. 웹의 동작을 구현하기 위한 언어입니다.
2. 웹 브라우저에서 사용합니다. (단, Node.js 는 서버에서 동작)
3. 웹 브라우저에는 자바스크립트 엔진(인터프리터)가 내장되어 있습니다.
4. HTML문서 내에서 <script> 태그 사이에 작성합니다.
5. 대소문자를 구별합니다.
6. 객체 기반의 스크립트 언어입니다.


자바스크립트의 출력
1. document.write() 함수를 사용해 <body>에 출력합니다.
2. console.log() 함수를 사용해 브라우저 console 창에 출력합니다.

* <script> 태그의 위치는 <head> 태그 또는 <body> 태그 사이에 작성합니다.


외부 자바스크립트 파일로 적용
<script src="자바스크립트 파일 경로"></script>

* 자바스크립트의 파일은 .js 확장자로 저장합니다.


자바스크립트의 주석문

// : 한 줄 주석문
/* */ : 여러줄 주석문


<!-- HTML 주석문 -->
/* CSS 주석문 */


변수(Variable)
변수란 데이터를 저장할 수 있는 메모리 공간을 의미합니다. 값은 변경될 수 있습니다. let 키워드를 사용해 변수를 선언하고 선언되지 않은 변수를 사용하려고 하거나 접근하려고 하면 오류가 발생합니다.

let 변수명;  //  변수의 선언
변수명 = 값; //  변수의 초기화

let num;
num = 10;
num = 20;

* use strict 은 strict mode 사용을 한다는 의미이며 코드를 엄격한 모드로 사용하게 합니다.
* var 키워드는 더이상 사용하지 않습니다.

var num;
num = 10;


let num = 10;	// 변수 선언 및 초기화


상수(constant)
한번 선언된 상수는 다시 재정의 할 수 없으며, 값을 재할당 할 수 없습니다.

const 상수명 = 값;


참고 사이트

https://www.w3schools.com/

https://developer.mozilla.org/ko/docs/Web/JavaScript



자바스크립트 데이터 타입
타입이란 프로그램에서 다룰 수 있는 값의 종류를 의미합니다.

1. 숫자형(number)
자바스크립트는 다른 언어와 달리 정수와 실수를 따로 구분하지 않습니다. 모든 수를 실수 하나로만 표현합니다.

2. 문자열형(string)
자바스크립트에서는 문자열을 쌍따옴표나 따옴표로 둘러싸인 문자의 집합을 의미합니다. 

let str1 = "류정원 : '여러분 반가워요'";
let str2 = '류정원 : "여러분 반가워요"';
let str3 = "\"안녕하세요. \n자바스크립트\"";
let str4 = `자바스크립트에서는 문자열을 쌍따옴표나 따옴표로 
	둘러싸인 문자의 집합을 의미합니다.`

3. 불리언형(boolean)
불리언 값은 true(참), false(거짓)으로 표현합니다. 

4. undefined, null 형
자바스크립트에서 null이란 object 타입이며, 아직 "값"이 정해지지 않은 것을 의미합니다. 또한 undefined란 null과는 달리 "타입"이 정해지지 않은 것을 의미합니다. 따라서 자바스크립트에서는 undefined는 초기화 되지 않은 변수나 존재하지 않은 값에 접근할 때 반환됩니다.

5. 심볼형(Symbol)
ECMAScript6에서 추가된 데이터타입입니다. 유일하고 변경 불가능한 기본값을 만듭니다. 또한 객체 속성의 key 값으로도 사용될 수 있습니다.

const sym1 = Symbol('a');
const sym2 = Symbol('a');

console.log(sym1 === sym2); // false

